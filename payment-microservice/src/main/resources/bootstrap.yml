spring:
  application:
    name: "payment-microservice"
  config:
    import: "configserver:"
  cloud:
    config:
      uri: "http://localhost:8080/config"
      profile: "default"
      fail-fast: true
eureka:
  instance:
    instance-id: "payment-microservice-${random.value}"