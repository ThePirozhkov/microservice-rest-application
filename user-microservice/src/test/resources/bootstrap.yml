spring:
  application:
    name: user-microservice
  config:
    import: "configserver:"
  cloud:
    config:
      uri: "http://localhost:8080/config"
      profile: "test"
      fail-fast: true
logging:
  level:
    org.springframework.web: DEBUG